[
  {
    "name": "items",
    "type": "array",
    "required": true,
    "description": "The items within breadcrumbs.",
    "params": [
      {
        "name": "text",
        "type": "string",
        "required": true,
        "description": "Text to use within the breadcrumbs item."
      },
      {
        "name": "href",
        "type": "string",
        "required": true,
        "description": "Link for the breadcrumbs item."
      },
      {
        "name": "title",
        "type": "string",
        "required": false,
        "description": "Optional title for the breadcrumb item used in the title attribute."
      }
    ]
  },
  {
    "name": "noCollapse",
    "type": "string",
    "required": false,
    "description": "When true, the breadcrumbs will not collapse to the first and last item only on smaller devices."
  },
  {
    "name": "structuredData",
    "type": "boolean",
    "required": false,
    "description": "When true, structured data markup is added to the breadcrumbs."
  },
  {
    "name": "classes",
    "type": "string",
    "required": false,
    "description": "Classes to add to the breadcrumbs."
  },
  {
    "name": "attributes",
    "type": "object",
    "required": false,
    "description": "HTML attributes (for example data attributes) to add to the breadcrumbs."
  }
]
